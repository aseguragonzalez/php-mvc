{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"PHP MVC","text":"<p>Lightweight PHP 8.4+ MVC framework. Provides a PSR-7/PSR-15 web layer, a CLI scaffolding tool, and optional modules for database migrations, background tasks, and authentication.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>PSR-7 / PSR-15 \u2014 bring your own HTTP foundation; the framework adapts to your stack.</li> <li>Typed routing \u2014 path parameters coerced to <code>int</code>, <code>float</code>, <code>string</code>, or validated UUID.</li> <li>Auto-escaped views \u2014 <code>{{var}}</code> is HTML-escaped by default; raw HTML requires <code>{{{var}}}</code>.</li> <li>CSRF protection \u2014 token validated from form body or <code>X-CSRF-Token</code> header.</li> <li>Cookie-based auth \u2014 sign-up, sign-in, password reset, and session refresh use cases included.</li> <li>Database migrations \u2014 timestamped SQL folders with forward/rollback scripts and schema comparison.</li> <li>Background tasks \u2014 SQL-backed task queue with cron or in-process worker modes.</li> <li>Asset bundling \u2014 merge and minify JS/CSS with a single CLI command.</li> <li>PHPStan max \u2014 strictly typed throughout.</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>composer require aseguragonzalez/php-mvc\n</code></pre> <p>Requires PHP 8.4 and Composer.</p>"},{"location":"#where-to-start","title":"Where to start","text":"<ul> <li>Installation \u2014 requirements, install, verify.</li> <li>Quickstart \u2014 scaffold your first app in minutes.</li> <li>Architecture \u2014 understand how the modules fit together.</li> <li>CLI Reference \u2014 every <code>mvc</code> command in one place.</li> </ul>"},{"location":"#license","title":"License","text":"<p>MIT License. Copyright (c) 2026 Alfonso Segura.</p>"},{"location":"cli/reference/","title":"CLI Reference","text":"<p>The <code>mvc</code> binary provides scaffolding, code generation, and development tooling. Run it via <code>vendor/bin/mvc</code>.</p>"},{"location":"cli/reference/#app-scaffolding","title":"App scaffolding","text":""},{"location":"cli/reference/#mvc-create-app","title":"<code>mvc create-app</code>","text":"<p>Scaffold a new MVC application structure.</p> <pre><code>vendor/bin/mvc create-app &lt;path&gt; --name=&lt;AppName&gt; --namespace=&lt;Namespace&gt;\n</code></pre> Option Required Description <code>&lt;path&gt;</code> Yes Target directory for the new app. <code>--name</code> Yes App class name (e.g. <code>MyApp</code>). Used for generated class names. <code>--namespace</code> Yes PHP namespace (e.g. <code>App\\\\MyApp</code>). <p>Generates: folder structure, <code>mvc.config.json</code>, <code>{Name}App.php</code>, <code>{Name}Bootstrap.php</code>, i18n file, and asset directories.</p>"},{"location":"cli/reference/#database-migrations","title":"Database migrations","text":""},{"location":"cli/reference/#mvc-migrationsenable","title":"<code>mvc migrations:enable</code>","text":"<p>Enable the migrations module for an app.</p> <pre><code>vendor/bin/mvc migrations:enable [--path=&lt;app-dir&gt;] [--folder=&lt;folder&gt;]\n</code></pre> Option Default Description <code>--path</code> <code>.</code> MVC app root (contains <code>index.php</code> + <code>mvc.config.json</code>). <code>--folder</code> <code>Migrations</code> Migration module folder name under the app root. <p>Creates the migration folder structure, writes <code>index.php</code>, and sets <code>migrationsEnabled: true</code> in <code>mvc.config.json</code>.</p> <p>Alias: <code>mvc initialize-migrations</code> (deprecated).</p>"},{"location":"cli/reference/#mvc-migrationsdisable","title":"<code>mvc migrations:disable</code>","text":"<p>Disable the migrations module.</p> <pre><code>vendor/bin/mvc migrations:disable [--path=&lt;app-dir&gt;] [--remove-files] [--force]\n</code></pre> Option Description <code>--path</code> App root (default: current directory). <code>--remove-files</code> Delete migration module files from disk. Requires <code>--force</code>. <code>--force</code> Confirm destructive file removal."},{"location":"cli/reference/#mvc-migrationscreate","title":"<code>mvc migrations:create</code>","text":"<p>Create a new timestamped migration folder with blank forward and rollback scripts.</p> <pre><code>vendor/bin/mvc migrations:create --app-path=&lt;app-dir&gt;\n# or override the migrations directory directly:\nvendor/bin/mvc migrations:create --path=&lt;migrations-dir&gt;\n</code></pre>"},{"location":"cli/reference/#mvc-migrationsrun","title":"<code>mvc migrations:run</code>","text":"<p>Apply all pending migrations in chronological order.</p> <pre><code>vendor/bin/mvc migrations:run --app-path=&lt;app-dir&gt;\n</code></pre>"},{"location":"cli/reference/#mvc-migrationstest","title":"<code>mvc migrations:test</code>","text":"<p>Test a single migration: apply \u2192 rollback \u2192 schema comparison.</p> <pre><code>vendor/bin/mvc migrations:test --app-path=&lt;app-dir&gt; --migration=&lt;folder-name&gt;\n</code></pre> Option Description <code>--migration</code> Timestamped folder name (e.g. <code>20260101120000</code>)."},{"location":"cli/reference/#authentication","title":"Authentication","text":""},{"location":"cli/reference/#mvc-authenable","title":"<code>mvc auth:enable</code>","text":"<p>Enable the authentication module; optionally generate SQL migrations for the default user/session tables.</p> <pre><code>vendor/bin/mvc auth:enable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre> Option Description <code>--skip-migrations</code> Only set <code>authenticationEnabled: true</code>. No SQL files created. <p>Prerequisite: the migrations module must be enabled.</p>"},{"location":"cli/reference/#mvc-authdisable","title":"<code>mvc auth:disable</code>","text":"<p>Disable the authentication module.</p> <pre><code>vendor/bin/mvc auth:disable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre> <p>Without <code>--skip-migrations</code>, generates a migration to drop the default auth tables.</p>"},{"location":"cli/reference/#background-tasks","title":"Background tasks","text":""},{"location":"cli/reference/#mvc-initialize-background-tasks","title":"<code>mvc initialize-background-tasks</code>","text":"<p>Scaffold the BackgroundTasks folder and stubs. Sets <code>backgroundTasksEnabled: false</code>.</p> <pre><code>vendor/bin/mvc initialize-background-tasks [--path=&lt;app-dir&gt;]\n</code></pre>"},{"location":"cli/reference/#mvc-background-tasksenable","title":"<code>mvc background-tasks:enable</code>","text":"<p>Enable the background tasks module; optionally generate the <code>background_tasks</code> migration.</p> <pre><code>vendor/bin/mvc background-tasks:enable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre> Option Description <code>--skip-migrations</code> Only set <code>backgroundTasksEnabled: true</code>. Use with a custom <code>TaskRepository</code>. <p>Prerequisite: the migrations module must be enabled (unless <code>--skip-migrations</code>).</p>"},{"location":"cli/reference/#mvc-background-tasksdisable","title":"<code>mvc background-tasks:disable</code>","text":"<p>Disable the background tasks module.</p> <pre><code>vendor/bin/mvc background-tasks:disable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre>"},{"location":"cli/reference/#mvc-background-tasksrun","title":"<code>mvc background-tasks:run</code>","text":"<p>Run the configured worker entrypoint (<code>BackgroundTasks/index.php</code>).</p> <pre><code>vendor/bin/mvc background-tasks:run [--app-path=&lt;app-dir&gt;] [--force] [-- &lt;args&gt;...]\n</code></pre> Option Description <code>--app-path</code> App root (default: current directory). <code>--force</code> Run even when <code>backgroundTasksEnabled</code> is <code>false</code>. <code>-- &lt;args&gt;</code> Arguments forwarded to <code>BackgroundTasks/index.php</code> (e.g. <code>-- --interval=60</code>)."},{"location":"cli/reference/#asset-bundling","title":"Asset bundling","text":""},{"location":"cli/reference/#mvc-watch-assets","title":"<code>mvc watch-assets</code>","text":"<p>Watch source files and rebuild unminified JS/CSS bundles on change (development).</p> <pre><code>vendor/bin/mvc watch-assets --app-path=&lt;app-dir&gt;\n</code></pre>"},{"location":"cli/reference/#mvc-create-bundle","title":"<code>mvc create-bundle</code>","text":"<p>Merge and minify all source JS/CSS into production bundles.</p> <pre><code>vendor/bin/mvc create-bundle --app-path=&lt;app-dir&gt;\n</code></pre>"},{"location":"cli/reference/#deprecated-commands","title":"Deprecated commands","text":"Command Replacement <code>mvc initialize-migrations</code> <code>mvc migrations:enable</code>"},{"location":"core-concepts/architecture/","title":"Architecture","text":"<p>PHP MVC is a modular MVC framework. The core web layer handles routing, controllers, views, and the middleware pipeline. Optional feature modules (Security, Migrations, BackgroundTasks) are self-contained and can be enabled independently via the CLI.</p>"},{"location":"core-concepts/architecture/#module-map","title":"Module map","text":"<pre><code>graph TD\n    subgraph \"PhpMvc\\\\ namespace\"\n        Web[\"Web\\nRouting \u00b7 Controllers \u00b7 Views\\nMiddleware \u00b7 Request handling\"]\n        Security[\"Functional/Security\\nSignUp \u00b7 SignIn \u00b7 ResetPassword\\nSessions \u00b7 Identity management\"]\n        Migrations[\"Apps/Migrations\\nSchema versioning\\nForward / Rollback scripts\"]\n        BgTasks[\"Apps/BackgroundTasks\\nTask queue\\nWorker loop\"]\n        CLI[\"Cli\\nScaffolding \u00b7 Code generation\\nmvc binary\"]\n        DevTools[\"DevTools\\nJS/CSS bundling\\nMinification\"]\n    end\n\n    PSR7[\"PSR-7\\nServerRequestInterface\\nResponseInterface\"]\n    PSR11[\"PSR-11\\nContainerInterface\"]\n    PSR15[\"PSR-15\\nMiddlewareInterface\\nRequestHandlerInterface\"]\n    PSR3[\"PSR-3\\nLoggerInterface\"]\n\n    Web --&gt; PSR7\n    Web --&gt; PSR15\n    Web --&gt; PSR11\n    Security --&gt; PSR11\n    Security --&gt; PSR3\n    Migrations --&gt; PSR3\n    BgTasks --&gt; PSR3\n    CLI --&gt; Web\n    CLI --&gt; Security\n    CLI --&gt; Migrations\n    CLI --&gt; BgTasks</code></pre>"},{"location":"core-concepts/architecture/#module-internals","title":"Module internals","text":"<p>Each feature module exposes interfaces that your application depends on, and ships concrete SQL-backed implementations you can swap out:</p> <pre><code>graph LR\n    I[\"Interfaces\\nContracts your app depends on\"]\n    UC[\"Use cases\\nApplication services\"]\n    SQL[\"SQL implementations\\nPDO-backed repositories\"]\n\n    UC --&gt;|uses| I\n    SQL --&gt;|implements| I</code></pre> <p>Register the SQL implementations in your container with a single call (e.g. <code>Security\\Dependencies::configure($container)</code>), or bind your own implementations if you use a different storage backend.</p>"},{"location":"core-concepts/architecture/#psr-compliance","title":"PSR compliance","text":"Standard Role PSR-4 Autoloading \u2014 all classes under <code>PhpMvc\\</code> PSR-7 HTTP messages \u2014 <code>ServerRequestInterface</code>, <code>ResponseInterface</code> PSR-11 Dependency injection container \u2014 <code>ContainerInterface</code> PSR-15 HTTP middleware \u2014 <code>MiddlewareInterface</code>, <code>RequestHandlerInterface</code> PSR-3 Logging \u2014 <code>LoggerInterface</code> <p>The framework depends only on the PSR interface packages (<code>psr/*</code>). You choose the concrete implementations.</p>"},{"location":"core-concepts/architecture/#namespace-structure","title":"Namespace structure","text":"<pre><code>PhpMvc\\\n\u251c\u2500\u2500 Web\\            \u2014 HTTP layer (routing, controllers, views, middleware)\n\u251c\u2500\u2500 Functional\\\n\u2502   \u2514\u2500\u2500 Security\\   \u2014 auth use cases, identity, sessions\n\u251c\u2500\u2500 Apps\\\n\u2502   \u251c\u2500\u2500 Migrations\\         \u2014 database migrations\n\u2502   \u2514\u2500\u2500 BackgroundTasks\\    \u2014 queued task processing\n\u251c\u2500\u2500 Cli\\            \u2014 scaffolding commands\n\u2514\u2500\u2500 DevTools\\       \u2014 JS/CSS asset builders\n</code></pre>"},{"location":"core-concepts/architecture/#design-principles","title":"Design principles","text":"<ul> <li>Security by default \u2014 inputs are normalized and HTML-escaped; CSRF protection is opt-in but trivial to enable.</li> <li>Predictable behavior \u2014 strong typing for action parameters and DTOs; no magic globals.</li> <li>Simple integration \u2014 everything wires through <code>MvcWebApp</code> and the PSR-11 container.</li> <li>No hidden state \u2014 middlewares and services are registered explicitly in the composition root before <code>run()</code>.</li> </ul>"},{"location":"core-concepts/request-lifecycle/","title":"Request Lifecycle","text":"<p>This page traces the path of an HTTP request through the framework from the moment PHP is invoked until the response is sent.</p>"},{"location":"core-concepts/request-lifecycle/#sequence-diagram","title":"Sequence diagram","text":"<pre><code>sequenceDiagram\n    participant B as Browser\n    participant E as index.php\n    participant App as MvcWebApp\n    participant EH as ErrorHandling\n    participant Loc as Localization\n    participant CSRF as CsrfProtection\n    participant Auth as Authentication\n    participant Authz as Authorization\n    participant RH as RequestHandler\n    participant C as Controller\n\n    B-&gt;&gt;E: HTTP request\n    E-&gt;&gt;E: build PSR-7 ServerRequest\n    E-&gt;&gt;App: handle(request)\n    App-&gt;&gt;EH: handle(request)\n    EH-&gt;&gt;Loc: handle(request)\n    Loc-&gt;&gt;CSRF: handle(request) [optional]\n    CSRF-&gt;&gt;Auth: handle(request) [optional]\n    Auth-&gt;&gt;Authz: handle(request) [optional]\n    Authz-&gt;&gt;RH: handle(request)\n    RH-&gt;&gt;RH: Router::get() \u2014 match route\n    RH-&gt;&gt;RH: ActionParameterBuilder \u2014 bind params\n    RH-&gt;&gt;C: invoke action\n    C--&gt;&gt;RH: ActionResponse\n    RH--&gt;&gt;Authz: PSR-7 Response\n    Authz--&gt;&gt;Auth: PSR-7 Response\n    Auth--&gt;&gt;CSRF: PSR-7 Response\n    CSRF--&gt;&gt;Loc: PSR-7 Response\n    Loc--&gt;&gt;EH: PSR-7 Response\n    EH--&gt;&gt;App: PSR-7 Response\n    App--&gt;&gt;E: PSR-7 Response\n    E-&gt;&gt;B: status + headers + body</code></pre>"},{"location":"core-concepts/request-lifecycle/#stage-by-stage-explanation","title":"Stage-by-stage explanation","text":""},{"location":"core-concepts/request-lifecycle/#1-entrypoint-indexphp","title":"1. Entrypoint (<code>index.php</code>)","text":"<p>Your composition root registers all services on the PSR-11 container, constructs the <code>ServerRequestInterface</code> from PHP globals, and passes it to <code>MvcWebApp::handle()</code>. Everything the middlewares and controllers need must already be in the container at this point. Once <code>handle()</code> returns, the entrypoint emits the response via <code>ResponseEmitter::emit()</code>.</p>"},{"location":"core-concepts/request-lifecycle/#2-mvcwebapphandle","title":"2. <code>MvcWebApp::handle()</code>","text":"<p>Implements <code>Psr\\Http\\Server\\RequestHandlerInterface</code>. Wires the middleware pipeline and delegates the incoming request to it, returning the <code>ResponseInterface</code> produced by the pipeline.</p>"},{"location":"core-concepts/request-lifecycle/#3-errorhandling-middleware","title":"3. <code>ErrorHandling</code> middleware","text":"<p>Wraps the entire remaining chain. Any uncaught exception is converted into an HTTP error response. The browser always receives a well-formed response even on fatal errors.</p>"},{"location":"core-concepts/request-lifecycle/#4-localization-middleware","title":"4. <code>Localization</code> middleware","text":"<p>Detects the request locale from <code>Accept-Language</code> or a cookie. The view engine uses this locale to resolve i18n keys automatically.</p>"},{"location":"core-concepts/request-lifecycle/#5-csrfprotection-middleware-optional","title":"5. <code>CsrfProtection</code> middleware (optional)","text":"<p>Enabled via <code>$app-&gt;useCsrfProtection()</code>. Generates and validates CSRF tokens. Returns <code>403 Forbidden</code> if a state-changing request (<code>POST</code>, <code>PUT</code>, <code>PATCH</code>, <code>DELETE</code>) arrives without a valid token.</p>"},{"location":"core-concepts/request-lifecycle/#6-authentication-middleware-optional","title":"6. <code>Authentication</code> middleware (optional)","text":"<p>Enabled via <code>$app-&gt;useAuthentication()</code>. Reads the auth cookie and loads the user identity. Does not block unauthenticated requests \u2014 that is <code>Authorization</code>'s job.</p>"},{"location":"core-concepts/request-lifecycle/#7-authorization-middleware-optional","title":"7. <code>Authorization</code> middleware (optional)","text":"<p>Enabled via <code>$app-&gt;useAuthorization()</code>. Checks <code>authRequired</code> and <code>roles</code> on the matched route. Redirects unauthenticated users to the sign-in page. Returns <code>403 Forbidden</code> for authenticated users lacking the required role.</p>"},{"location":"core-concepts/request-lifecycle/#8-request-handler","title":"8. Request handler","text":"<p>Matches the route, binds typed action parameters from path segments, query string, and request body, then invokes the controller action and converts its response to HTTP.</p>"},{"location":"core-concepts/request-lifecycle/#9-response-propagation","title":"9. Response propagation","text":"<p>Each middleware may add headers on the way back out (e.g. auth session refresh). <code>MvcWebApp::handle()</code> returns the final <code>ResponseInterface</code> to the entrypoint, which emits status code, headers, and body via <code>ResponseEmitter::emit()</code>.</p>"},{"location":"core-concepts/request-lifecycle/#adding-custom-middleware","title":"Adding custom middleware","text":"<p>Custom middlewares run inside the fixed outer chain (after <code>Localization</code>, before <code>RequestHandler</code>):</p> <pre><code>$app-&gt;addMiddleware(MyRateLimitMiddleware::class);\n</code></pre> <p>See Middleware for the full <code>MiddlewareInterface</code> contract.</p>"},{"location":"getting-started/configuration/","title":"Configuration Reference","text":"<p>All per-app settings live in <code>mvc.config.json</code>, placed next to the app's <code>index.php</code>. The file is generated by <code>mvc create-app</code> and updated by other CLI commands (<code>mvc migrations:enable</code>, <code>mvc auth:enable</code>, etc.).</p>"},{"location":"getting-started/configuration/#full-key-reference","title":"Full key reference","text":"Key Type Default Description <code>jsAssetsPath</code> string <code>./assets/scripts</code> Directory where JS bundles are written, relative to the app root. <code>cssAssetsPath</code> string <code>./assets/styles</code> Directory where CSS bundles are written. <code>mainJsBundler</code> string <code>main.min.js</code> Filename for the minified JS bundle produced by <code>mvc create-bundle</code>. Reference this in production layouts. <code>mainCssBundler</code> string <code>main.min.css</code> Filename for the minified CSS bundle. <code>devMainJsBundler</code> string <code>main.js</code> Filename for the unminified JS bundle produced by <code>mvc watch-assets</code>. <code>devMainCssBundler</code> string <code>main.css</code> Filename for the unminified CSS bundle. <code>useDevAssets</code> bool <code>false</code> When <code>true</code>, layouts load <code>devMainJsBundler</code>/<code>devMainCssBundler</code> instead of the minified files. Set to <code>true</code> while running <code>mvc watch-assets</code> locally. <code>assetRoutes</code> array <code>[{\"label\":\"default\",\"js\":[...],\"css\":[...]}]</code> Ordered groups of source files to merge into bundles. See Asset Bundling. <code>i18nBasePath</code> string <code>./assets/i18n</code> Directory containing <code>{locale}.json</code> translation files. <code>migrationsFolderPath</code> string <code>\"\"</code> Relative path from the app root to the migration module directory. Set by <code>mvc migrations:enable</code>. <code>migrationsEnabled</code> bool <code>false</code> <code>true</code> when migrations are active. The migration module's <code>index.php</code> enforces this. <code>backgroundTasksFolderPath</code> string <code>\"\"</code> Relative path to the BackgroundTasks module. Set by <code>mvc initialize-background-tasks</code>. <code>backgroundTasksEnabled</code> bool <code>false</code> <code>true</code> after <code>mvc background-tasks:enable</code>. The worker's <code>index.php</code> enforces this. <code>backgroundTasksPollIntervalSeconds</code> int <code>0</code> When <code>&gt; 0</code>, the worker runs in a continuous loop, sleeping this many seconds between batches. When <code>0</code>, one batch per invocation. <code>authenticationEnabled</code> bool <code>false</code> <code>true</code> after <code>mvc auth:enable</code>. Used by the entrypoint to conditionally call <code>useAuthentication()</code> / <code>useAuthorization()</code>."},{"location":"getting-started/configuration/#example","title":"Example","text":"<pre><code>{\n  \"jsAssetsPath\": \"./assets/scripts\",\n  \"mainJsBundler\": \"main.min.js\",\n  \"devMainJsBundler\": \"main.js\",\n  \"cssAssetsPath\": \"./assets/styles\",\n  \"mainCssBundler\": \"main.min.css\",\n  \"devMainCssBundler\": \"main.css\",\n  \"useDevAssets\": false,\n  \"assetRoutes\": [\n    {\n      \"label\": \"base\",\n      \"js\": [\"assets/scripts/core.js\"],\n      \"css\": [\"assets/styles/root.css\", \"assets/styles/layout.css\"]\n    },\n    {\n      \"label\": \"dashboard\",\n      \"js\": [\"assets/scripts/dashboard.js\"],\n      \"css\": [\"assets/styles/dashboard.css\"]\n    }\n  ],\n  \"i18nBasePath\": \"./assets/i18n\",\n  \"migrationsFolderPath\": \"./Migrations\",\n  \"migrationsEnabled\": true,\n  \"backgroundTasksFolderPath\": \"./BackgroundTasks\",\n  \"backgroundTasksEnabled\": true,\n  \"backgroundTasksPollIntervalSeconds\": 0,\n  \"authenticationEnabled\": true\n}\n</code></pre>"},{"location":"getting-started/configuration/#loading-config-at-runtime","title":"Loading config at runtime","text":"<pre><code>use PhpMvc\\Config\\MvcConfig;\n\n$config = MvcConfig::load($basePath);\n\nif ($config-&gt;isAuthenticationEnabled()) {\n    $app-&gt;useAuthentication();\n    $app-&gt;useAuthorization();\n}\n</code></pre>"},{"location":"getting-started/configuration/#related-documentation","title":"Related documentation","text":"<ul> <li>Asset Bundling \u2014 <code>assetRoutes</code> and the watch/bundle workflow.</li> <li>Database Migrations \u2014 <code>migrationsFolderPath</code> and <code>migrationsEnabled</code>.</li> <li>Background Tasks \u2014 <code>backgroundTasksFolderPath</code>, <code>backgroundTasksEnabled</code>, <code>backgroundTasksPollIntervalSeconds</code>.</li> <li>Authentication \u2014 <code>authenticationEnabled</code> and middleware wiring.</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"Requirement Version PHP 8.4 Composer 2.x"},{"location":"getting-started/installation/#install-the-package","title":"Install the package","text":"<pre><code>composer require aseguragonzalez/php-mvc\n</code></pre>"},{"location":"getting-started/installation/#verify-the-cli","title":"Verify the CLI","text":"<p>The package ships with the <code>mvc</code> binary:</p> <pre><code>vendor/bin/mvc --help\n</code></pre> <p>You should see the list of available commands. If you prefer a shorter path, add <code>vendor/bin</code> to your <code>PATH</code>.</p>"},{"location":"getting-started/installation/#psr-11-container","title":"PSR-11 container","text":"<p>The framework resolves controllers, middlewares, and services through a PSR-11 container. The package depends only on <code>psr/container</code> \u2014 you choose the implementation. <code>php-di/php-di</code> is a common choice:</p> <pre><code>composer require php-di/php-di\n</code></pre> <p>The scaffolded bootstrap (generated by <code>mvc create-app</code>) wires <code>DI\\Container</code> by default.</p>"},{"location":"getting-started/installation/#psr-7-http-foundation","title":"PSR-7 HTTP foundation","text":"<p>The framework's <code>MvcWebApp</code> reads globals with <code>$_SERVER</code>, <code>$_GET</code>, <code>$_POST</code>, and <code>$_FILES</code> to build an internal PSR-7 request. You do not need to install a separate PSR-7 implementation unless your code interacts with PSR-7 objects directly.</p>"},{"location":"getting-started/installation/#next-step","title":"Next step","text":"<p>Quickstart \u2192 \u2014 scaffold a new app and handle your first request.</p>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>This guide takes you from zero to a running MVC app in five steps.</p>"},{"location":"getting-started/quickstart/#1-scaffold-the-app","title":"1. Scaffold the app","text":"<pre><code>vendor/bin/mvc create-app ./src/MyApp --name=MyApp --namespace=App\\\\MyApp\n</code></pre> <p>This generates:</p> <pre><code>src/MyApp/\n\u251c\u2500\u2500 Controllers/\n\u251c\u2500\u2500 Views/\n\u251c\u2500\u2500 assets/\n\u2502   \u251c\u2500\u2500 i18n/\n\u2502   \u2502   \u2514\u2500\u2500 en.json\n\u2502   \u251c\u2500\u2500 scripts/\n\u2502   \u2502   \u2514\u2500\u2500 main.js\n\u2502   \u2514\u2500\u2500 styles/\n\u2502       \u2514\u2500\u2500 main.css\n\u251c\u2500\u2500 MyAppApp.php\n\u251c\u2500\u2500 MyAppBootstrap.php\n\u2514\u2500\u2500 mvc.config.json\n</code></pre> <ul> <li><code>MyAppApp.php</code> \u2014 extends <code>MvcWebApp</code>; wire routes here.</li> <li><code>MyAppBootstrap.php</code> \u2014 composition root; register settings, PDO, and services on the container.</li> <li><code>mvc.config.json</code> \u2014 feature flags and paths (see Configuration Reference).</li> </ul>"},{"location":"getting-started/quickstart/#2-define-a-controller","title":"2. Define a controller","text":"<pre><code>&lt;?php\n\ndeclare(strict_types=1);\n\nnamespace App\\MyApp\\Controllers;\n\nuse PhpMvc\\Controllers\\Controller;\nuse PhpMvc\\Actions\\Responses\\ActionResponse;\nuse PhpMvc\\Actions\\Responses\\View;\n\nfinal class HomeController extends Controller\n{\n    public function index(): ActionResponse\n    {\n        return $this-&gt;view();\n    }\n\n    public function show(int $id): ActionResponse\n    {\n        $model = (object) ['id' =&gt; $id, 'title' =&gt; \"Item {$id}\"];\n        return $this-&gt;view(model: $model);\n    }\n}\n</code></pre>"},{"location":"getting-started/quickstart/#3-register-routes","title":"3. Register routes","text":"<p>Open <code>MyAppApp.php</code> and register routes in the <code>router()</code> method:</p> <pre><code>&lt;?php\n\ndeclare(strict_types=1);\n\nnamespace App\\MyApp;\n\nuse PhpMvc\\MvcWebApp;\nuse PhpMvc\\Routes\\Router;\nuse PhpMvc\\Routes\\Route;\nuse PhpMvc\\Routes\\RouteMethod;\nuse PhpMvc\\Routes\\Path;\nuse App\\MyApp\\Controllers\\HomeController;\n\nfinal class MyAppApp extends MvcWebApp\n{\n    protected function router(): Router\n    {\n        $router = new Router();\n\n        $router-&gt;register(Route::create(\n            RouteMethod::Get,\n            Path::create('/'),\n            HomeController::class,\n            'index',\n        ));\n\n        $router-&gt;register(Route::create(\n            RouteMethod::Get,\n            Path::create('/items/{int:id}'),\n            HomeController::class,\n            'show',\n        ));\n\n        return $router;\n    }\n}\n</code></pre>"},{"location":"getting-started/quickstart/#4-create-a-view","title":"4. Create a view","text":"<p>Create <code>src/MyApp/Views/Home/index.html</code>:</p> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;&lt;title&gt;My App&lt;/title&gt;&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;Hello from PHP MVC&lt;/h1&gt;\n  &lt;a href=\"/items/1\"&gt;View item 1&lt;/a&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p>And <code>src/MyApp/Views/Home/show.html</code>:</p> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;&lt;title&gt;Item {{model-&gt;id}}&lt;/title&gt;&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;{{model-&gt;title}}&lt;/h1&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre>"},{"location":"getting-started/quickstart/#5-create-the-entrypoint","title":"5. Create the entrypoint","text":"<p>Create <code>public/index.php</code>:</p> <pre><code>&lt;?php\n\ndeclare(strict_types=1);\n\nrequire_once __DIR__ . '/../vendor/autoload.php';\n\nuse DI\\Container;\nuse PhpMvc\\ResponseEmitter;\nuse Psr\\Http\\Message\\ServerRequestFactoryInterface;\nuse App\\MyApp\\MyAppApp;\nuse App\\MyApp\\MyAppBootstrap;\n\n$container = new Container();\nMyAppBootstrap::register($container, __DIR__ . '/../');\n$app = new MyAppApp(container: $container, basePath: __DIR__ . '/../');\n\n$server = $_SERVER;\n$method = is_string($server['REQUEST_METHOD'] ?? null) ? $server['REQUEST_METHOD'] : 'GET';\n$scheme = (!empty($server['HTTPS']) &amp;&amp; 'off' !== $server['HTTPS']) ? 'https' : 'http';\n$host = is_string($server['HTTP_HOST'] ?? $server['SERVER_NAME'] ?? null)\n    ? ($server['HTTP_HOST'] ?? $server['SERVER_NAME'])\n    : 'localhost';\n$rawUri = $server['REQUEST_URI'] ?? null;\n$uri = $scheme.'://'.$host.(is_string($rawUri) ? $rawUri : '/');\n\n/** @var ServerRequestFactoryInterface $requestFactory */\n$requestFactory = $container-&gt;get(ServerRequestFactoryInterface::class);\n$request = $requestFactory-&gt;createServerRequest($method, $uri, $server);\n\nResponseEmitter::emit($app-&gt;handle($request));\n</code></pre>"},{"location":"getting-started/quickstart/#6-run-the-built-in-server","title":"6. Run the built-in server","text":"<pre><code>php -S localhost:8080 -t public\n</code></pre> <p>Open <code>http://localhost:8080</code> \u2014 you should see Hello from PHP MVC.</p>"},{"location":"getting-started/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Configuration Reference \u2014 learn every <code>mvc.config.json</code> key.</li> <li>Routing \u2014 typed path parameters, auth-protected routes.</li> <li>Views &amp; Templates \u2014 template language reference.</li> <li>CLI Reference \u2014 all <code>mvc</code> commands.</li> </ul>"},{"location":"modules/assets/","title":"Asset Bundling","text":"<p>The CLI can merge and optionally minify your app's JavaScript and CSS source files into single bundles for the browser. Use <code>mvc watch-assets</code> during development and <code>mvc create-bundle</code> for production.</p>"},{"location":"modules/assets/#configuration-mvcconfigjson","title":"Configuration (<code>mvc.config.json</code>)","text":""},{"location":"modules/assets/#output-locations-and-filenames","title":"Output locations and filenames","text":"Key Default Description <code>jsAssetsPath</code> <code>./assets/scripts</code> Directory where JS bundles are written. <code>cssAssetsPath</code> <code>./assets/styles</code> Directory where CSS bundles are written. <code>mainJsBundler</code> <code>main.min.js</code> Minified JS bundle (production). Reference in layouts. <code>mainCssBundler</code> <code>main.min.css</code> Minified CSS bundle (production). <code>devMainJsBundler</code> <code>main.js</code> Unminified JS bundle (development). <code>devMainCssBundler</code> <code>main.css</code> Unminified CSS bundle (development). <code>useDevAssets</code> <code>false</code> When <code>true</code>, layouts load dev bundles instead of minified files."},{"location":"modules/assets/#source-lists-assetroutes","title":"Source lists: <code>assetRoutes</code>","text":"<p><code>assetRoutes</code> is an ordered array of source groups. The CLI merges all <code>js</code> and <code>css</code> paths in order into a single JS bundle and a single CSS bundle. Duplicate paths are included only once (first occurrence wins).</p> <pre><code>{\n  \"assetRoutes\": [\n    {\n      \"label\": \"base\",\n      \"js\":  [\"assets/scripts/core.js\"],\n      \"css\": [\"assets/styles/root.css\", \"assets/styles/layout.css\"]\n    },\n    {\n      \"label\": \"dashboard\",\n      \"js\":  [\"assets/scripts/dashboard.js\"],\n      \"css\": [\"assets/styles/dashboard.css\"]\n    }\n  ]\n}\n</code></pre> <p>You may omit <code>js</code> or <code>css</code> on any group if that side is empty.</p>"},{"location":"modules/assets/#commands","title":"Commands","text":""},{"location":"modules/assets/#watch-development","title":"Watch (development)","text":"<pre><code>vendor/bin/mvc watch-assets --app-path=./src/MyApp\n</code></pre> <p>Polls source files and rebuilds unminified bundles when they change. Does not write minified files. Loops until interrupted.</p>"},{"location":"modules/assets/#production-bundle","title":"Production bundle","text":"<pre><code>vendor/bin/mvc create-bundle --app-path=./src/MyApp\n</code></pre> <p>Merges and minifies all sources into <code>mainJsBundler</code> and <code>mainCssBundler</code>.</p>"},{"location":"modules/assets/#development-workflow","title":"Development workflow","text":"<pre><code>flowchart LR\n    Edit[\"Edit source\\n.js / .css\"]\n    Watch[\"mvc watch-assets\\nrunning in background\"]\n    Bundle[\"Unminified bundle\\nmain.js / main.css\"]\n    Browser[\"Browser reloads\"]\n\n    Edit --&gt; Watch --&gt; Bundle --&gt; Browser</code></pre> <ol> <li>Set <code>useDevAssets: true</code> in <code>mvc.config.json</code>.</li> <li>In one terminal, run <code>mvc watch-assets --app-path=&lt;your-app&gt;</code>.</li> <li>In another, start your PHP server.</li> <li>Edit source files; the watcher rebuilds the dev bundles automatically.</li> <li>Before deploying: set <code>useDevAssets: false</code>, run <code>mvc create-bundle</code>, deploy.</li> </ol>"},{"location":"modules/assets/#serving-bundles-in-html","title":"Serving bundles in HTML","text":"<p>Your layout template references the bundle filenames via the <code>UiAssetsSettings</code> context values:</p> <pre><code>&lt;link rel=\"stylesheet\" href=\"/{{mainCssBundler}}\"&gt;\n&lt;!-- ... --&gt;\n&lt;script src=\"/{{mainJsBundler}}\"&gt;&lt;/script&gt;\n</code></pre> <p>When <code>useDevAssets: true</code>, the template variables resolve to <code>devMainCssBundler</code>/<code>devMainJsBundler</code>. When <code>false</code>, they resolve to the minified names.</p>"},{"location":"modules/assets/#scaffolded-apps","title":"Scaffolded apps","text":"<p>Running <code>mvc create-app</code> generates:</p> <ul> <li><code>assets/scripts/main.js</code> \u2014 empty JS entry point.</li> <li><code>assets/styles/main.css</code> \u2014 empty CSS entry point.</li> <li>A default <code>assetRoutes</code> entry pointing at these files.</li> </ul> <p>So <code>mvc watch-assets</code> and <code>mvc create-bundle</code> work out of the box after scaffolding.</p>"},{"location":"modules/assets/#related-documentation","title":"Related documentation","text":"<ul> <li>Configuration Reference \u2014 full <code>mvc.config.json</code> key reference.</li> <li>CLI Reference \u2014 <code>watch-assets</code> and <code>create-bundle</code> flags.</li> </ul>"},{"location":"modules/background-tasks/","title":"Background Tasks","text":"<p>The background tasks module provides a SQL-backed task queue for work that should run outside the web request \u2014 sending emails, generating reports, processing uploads, and so on.</p>"},{"location":"modules/background-tasks/#enable","title":"Enable","text":"<p>Migrations must be enabled first (the default SQL storage requires a table):</p> <pre><code>vendor/bin/mvc migrations:enable --path=&lt;app-dir&gt;\n</code></pre> <p>Then scaffold and enable the module:</p> <pre><code># Step 1: scaffold the BackgroundTasks folder and stubs\nvendor/bin/mvc initialize-background-tasks [--path=&lt;app-dir&gt;]\n\n# Step 2: enable and generate the migration for background_tasks table\nvendor/bin/mvc background-tasks:enable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre> Option Description <code>--path</code> App root (default: current directory). <code>--skip-migrations</code> Only set <code>backgroundTasksEnabled: true</code>. Use when providing a custom <code>TaskRepository</code>. <p>Apply the generated migration:</p> <pre><code>vendor/bin/mvc migrations:run --app-path=&lt;app-dir&gt;\n</code></pre>"},{"location":"modules/background-tasks/#disable","title":"Disable","text":"<pre><code>vendor/bin/mvc background-tasks:disable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre> <p>Without <code>--skip-migrations</code>, a new migration is generated to drop the <code>background_tasks</code> table. Run <code>migrations:run</code> to apply it.</p>"},{"location":"modules/background-tasks/#environment-variables","title":"Environment variables","text":"<p>The generated bootstrap registers database and logging from environment variables:</p> Variable Description <code>BACKGROUND_TASKS_DATABASE_HOST</code> Database host. <code>BACKGROUND_TASKS_DATABASE_NAME</code> Database name. <code>BACKGROUND_TASKS_DATABASE_USER</code> Database user. <code>BACKGROUND_TASKS_DATABASE_PASSWORD</code> Database password. <code>BACKGROUND_TASKS_LOG_LEVEL</code> PSR-3 log level (e.g. <code>debug</code>, <code>info</code>, <code>error</code>)."},{"location":"modules/background-tasks/#registering-and-processing-tasks","title":"Registering and processing tasks","text":""},{"location":"modules/background-tasks/#register-a-task-from-a-web-request-or-another-task","title":"Register a task (from a web request or another task)","text":"<pre><code>use PhpMvc\\BackgroundTasks\\Application\\RegisterTask\\RegisterTask;\n\n$registerTask-&gt;execute(\n    type: 'send_welcome_email',\n    payload: ['userId' =&gt; 42, 'email' =&gt; 'user@example.com'],\n);\n</code></pre>"},{"location":"modules/background-tasks/#process-tasks-worker","title":"Process tasks (worker)","text":"<p>Implement a handler for each task type in your <code>BackgroundTasks/</code> composition root:</p> <pre><code>// BackgroundTasksBootstrap.php\n$taskBus-&gt;register('send_welcome_email', new SendWelcomeEmailHandler($mailer));\n</code></pre>"},{"location":"modules/background-tasks/#running-the-worker","title":"Running the worker","text":"<pre><code>vendor/bin/mvc background-tasks:run [--app-path=&lt;app-dir&gt;] [--force] [-- &lt;args&gt;...]\n</code></pre> <ul> <li><code>--force</code> \u2014 run even when <code>backgroundTasksEnabled</code> is <code>false</code>.</li> <li>Arguments after <code>--</code> are forwarded to <code>BackgroundTasks/index.php</code>.</li> </ul>"},{"location":"modules/background-tasks/#cron-vs-in-process-loop","title":"Cron vs in-process loop","text":"<pre><code>flowchart TD\n    subgraph \"Cron / external scheduler\"\n        C1[\"Cron triggers\\nmvc background-tasks:run\"] --&gt; B1[\"Process one batch\"]\n        B1 --&gt; C2[\"Cron triggers again\\nnext interval\"]\n    end\n\n    subgraph \"In-process loop\"\n        L1[\"Start worker\\n--interval=60\"] --&gt; L2[\"Process batch\"]\n        L2 --&gt; L3[\"Sleep 60s\"]\n        L3 --&gt; L2\n        L3 --&gt;|SIGINT / SIGTERM| L4[\"Graceful shutdown\\nafter current batch\"]\n    end</code></pre> <p>Cron mode (default): each invocation processes one batch and exits. Schedule with <code>crontab</code> or a job scheduler.</p> <p>Loop mode: pass <code>--interval=&lt;seconds&gt;</code> to the entrypoint (via <code>-- --interval=60</code>) or set <code>backgroundTasksPollIntervalSeconds</code> in <code>mvc.config.json</code>. The process loops indefinitely, sleeping between batches. On POSIX systems with <code>pcntl</code>, <code>SIGINT</code>/<code>SIGTERM</code> trigger a graceful shutdown after the current batch completes.</p>"},{"location":"modules/background-tasks/#custom-taskrepository","title":"Custom <code>TaskRepository</code>","text":"<p>To use a different storage backend (Redis, a cloud queue, etc.):</p> <ol> <li> <p>Enable without migrations:</p> <pre><code>vendor/bin/mvc background-tasks:enable --skip-migrations\n</code></pre> </li> <li> <p>Implement <code>TaskRepository</code>:</p> <pre><code>use PhpMvc\\BackgroundTasks\\Domain\\TaskRepository;\n\nfinal class RedisTaskRepository implements TaskRepository\n{\n    // ...\n}\n</code></pre> </li> <li> <p>Register in the BackgroundTasks composition root instead of the default SQL wiring:</p> <pre><code>$container-&gt;set(TaskRepository::class, RedisTaskRepository::class);\n</code></pre> </li> </ol>"},{"location":"modules/background-tasks/#related-documentation","title":"Related documentation","text":"<ul> <li>Database Migrations \u2014 prerequisite for default SQL storage.</li> <li>CLI Reference \u2014 all background-tasks command flags.</li> <li>Identity Manager \u2014 example of using background tasks for challenge notifications.</li> </ul>"},{"location":"modules/migrations/","title":"Database Migrations","text":"<p>The migrations module provides SQL schema versioning with timestamped migration folders, forward and rollback scripts, database backups, and schema comparison for testing.</p>"},{"location":"modules/migrations/#enable","title":"Enable","text":"<p>From the app root (where <code>index.php</code> and <code>mvc.config.json</code> live):</p> <pre><code>vendor/bin/mvc migrations:enable [--path=&lt;app-dir&gt;] [--folder=&lt;module-folder&gt;]\n</code></pre> Option Default Description <code>--path</code> current directory MVC app root containing <code>index.php</code> and <code>mvc.config.json</code>. <code>--folder</code> <code>Migrations</code> Name of the migration module folder under the app root. <p>This creates <code>&lt;app&gt;/&lt;folder&gt;/migrations/</code>, writes <code>&lt;app&gt;/&lt;folder&gt;/index.php</code>, and sets <code>migrationsFolderPath</code> and <code>migrationsEnabled: true</code> in <code>mvc.config.json</code>.</p>"},{"location":"modules/migrations/#disable","title":"Disable","text":"<pre><code>vendor/bin/mvc migrations:disable [--path=&lt;app-dir&gt;] [--remove-files] [--force]\n</code></pre> <p>Sets <code>migrationsEnabled: false</code> and clears <code>migrationsFolderPath</code>. By default no files are deleted.</p> <ul> <li><code>--remove-files</code> \u2014 delete the migration module directory from disk. Must be combined with <code>--force</code>.</li> </ul>"},{"location":"modules/migrations/#folder-layout","title":"Folder layout","text":"<pre><code>graph LR\n    App[\"app/\\nindex.php\\nmvc.config.json\"]\n    Mod[\"Migrations/\\nindex.php\"]\n    MDir[\"migrations/\"]\n    M1[\"20260101120000/\\n0001_migration.sql\\n0001_migration.rollback.sql\"]\n    M2[\"20260201090000/\\n0001_migration.sql\\n0001_migration.rollback.sql\"]\n\n    App --&gt; Mod --&gt; MDir --&gt; M1\n    MDir --&gt; M2</code></pre> <p>Each migration is a timestamped folder (<code>YYYYMMDDhhmmss</code>) containing:</p> <ul> <li><code>0001_migration.sql</code> \u2014 forward script (applied with <code>mvc migrations:run</code>).</li> <li><code>0001_migration.rollback.sql</code> \u2014 rollback script (applied to reverse the migration).</li> </ul>"},{"location":"modules/migrations/#environment-variables","title":"Environment variables","text":"<p>The migration runner reads database settings from environment variables:</p> Variable Description <code>MIGRATIONS_DATABASE_HOST</code> Database host. <code>MIGRATIONS_DATABASE_NAME</code> Database name. <code>MIGRATIONS_DATABASE_USER</code> Database user. <code>MIGRATIONS_DATABASE_PASSWORD</code> Database password. <p>Set these before running migration commands.</p>"},{"location":"modules/migrations/#create-a-migration","title":"Create a migration","text":"<pre><code>vendor/bin/mvc migrations:create --app-path=&lt;app-dir&gt;\n</code></pre> <p>Creates a new timestamped folder under <code>migrations/</code> with blank forward and rollback scripts. Edit them to add your DDL.</p> <p>Override the migrations directory if needed (bypasses <code>mvc.config.json</code>):</p> <pre><code>vendor/bin/mvc migrations:create --path=&lt;migrations-dir&gt;\n</code></pre>"},{"location":"modules/migrations/#run-pending-migrations","title":"Run pending migrations","text":"<pre><code>vendor/bin/mvc migrations:run --app-path=&lt;app-dir&gt;\n</code></pre> <p>Applies all unapplied forward scripts in chronological order. The module records which migrations have been applied.</p>"},{"location":"modules/migrations/#test-a-migration","title":"Test a migration","text":"<p>Runs apply \u2192 rollback \u2192 schema comparison for a single migration folder:</p> <pre><code>vendor/bin/mvc migrations:test --app-path=&lt;app-dir&gt; --migration=&lt;folder-name&gt;\n</code></pre> <p><code>&lt;folder-name&gt;</code> is the timestamped folder name (e.g. <code>20260101120000</code>). The test verifies that applying and rolling back the migration leaves the schema in its original state.</p>"},{"location":"modules/migrations/#migration-apply-flow","title":"Migration apply flow","text":"<pre><code>flowchart TD\n    Start[\"mvc migrations:run\"]\n    Backup[\"Backup database schema\"]\n    Pending[\"Find unapplied migrations\\n(sorted by timestamp)\"]\n    Apply[\"Apply forward script\\n0001_migration.sql\"]\n    Record[\"Record migration as applied\"]\n    Done[\"All migrations applied\"]\n\n    Start --&gt; Backup --&gt; Pending --&gt; Apply --&gt; Record\n    Record --&gt;|more pending| Apply\n    Record --&gt;|none left| Done</code></pre>"},{"location":"modules/migrations/#best-practices","title":"Best practices","text":"<ul> <li>Write rollback scripts \u2014 every forward script should have a matching rollback. This enables <code>migrations:test</code> and safe production rollbacks.</li> <li>One concern per migration \u2014 create a new timestamped folder for each schema change. Avoid combining unrelated DDL in one migration.</li> <li>Test before deploying \u2014 run <code>migrations:test</code> on all new migrations in CI before applying to production.</li> <li>Never edit applied migrations \u2014 once a migration has been applied to any environment, treat it as immutable. Create a new migration to amend it.</li> </ul>"},{"location":"modules/migrations/#related-documentation","title":"Related documentation","text":"<ul> <li>CLI Reference \u2014 all migration command flags.</li> <li>Background Tasks \u2014 background tasks depend on migrations for the default SQL table.</li> <li>Authentication \u2014 <code>mvc auth:enable</code> generates auth migrations.</li> </ul>"},{"location":"security/authentication/","title":"Authentication","text":"<p>The framework provides cookie-based authentication backed by SQL repositories. The feature is opt-in and can be enabled or disabled via the CLI.</p>"},{"location":"security/authentication/#enable-with-the-cli","title":"Enable with the CLI","text":"<p>From the app root (where <code>index.php</code> and <code>mvc.config.json</code> live):</p> <pre><code>vendor/bin/mvc auth:enable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre> <p>Without <code>--skip-migrations</code>, the CLI creates a timestamped migration folder under your <code>migrations/</code> directory. The forward script creates the default auth tables (<code>users</code>, <code>user_roles</code>, sign-up/reset-password challenge tables, and session tables). The rollback script drops them.</p> <p>Prerequisite: the migrations module must already be enabled:</p> <pre><code>vendor/bin/mvc migrations:enable --path=&lt;app-dir&gt;\n</code></pre>"},{"location":"security/authentication/#disable","title":"Disable","text":"<pre><code>vendor/bin/mvc auth:disable [--path=&lt;app-dir&gt;] [--skip-migrations]\n</code></pre> <p>Without <code>--skip-migrations</code>, a new migration is created whose forward script drops the auth tables (rollback recreates them). Run <code>mvc migrations:run</code> when you want the teardown applied.</p>"},{"location":"security/authentication/#entrypoint-wiring","title":"Entrypoint wiring","text":"<p>After enabling in <code>mvc.config.json</code>, load the config at runtime and call <code>useAuthentication()</code> / <code>useAuthorization()</code>:</p> <pre><code>use PhpMvc\\Config\\MvcConfig;\n\n$config = MvcConfig::load($basePath);\nif ($config-&gt;isAuthenticationEnabled()) {\n    $app-&gt;useAuthentication();\n    $app-&gt;useAuthorization();\n}\n\n$app-&gt;run();\n</code></pre> <p>You can also call these unconditionally if you don't need the config flag:</p> <pre><code>$app-&gt;useAuthentication();\n$app-&gt;useAuthorization();\n$app-&gt;run();\n</code></pre>"},{"location":"security/authentication/#wiring-the-dependencies","title":"Wiring the dependencies","text":"<p>After enabling the feature, register the security dependencies in your composition root:</p> <pre><code>use PhpMvc\\Security\\Dependencies;\n\nDependencies::configure($container);\n</code></pre> <p>This wires all the authentication services. The database schema must match the migrations generated by <code>mvc auth:enable</code>.</p>"},{"location":"security/authentication/#use-cases","title":"Use cases","text":"<p>The security module ships these application use cases under <code>PhpMvc\\Security\\Application\\</code>:</p> Use case Description <code>SignUp</code> Register a new user; triggers a sign-up challenge. <code>SignIn</code> Authenticate with credentials; sets the auth cookie. <code>SignOut</code> Invalidate the session and clear the auth cookie. <code>RefreshSignInSession</code> Extend the session TTL on active requests. <code>RequestResetPassword</code> Initiate a password-reset flow; triggers a challenge. <code>ModifyUserIdentityPassword</code> Change password given the current password. <code>ActivateUserIdentity</code> Activate a newly registered user after challenge verification. <code>GetIdentity</code> Retrieve the current identity from context. <p>Wire them through the container and expose them via controllers.</p>"},{"location":"security/authentication/#related-documentation","title":"Related documentation","text":"<ul> <li>Authorization \u2014 role-based route protection.</li> <li>Identity Manager \u2014 custom <code>IdentityManager</code> and <code>ChallengeNotificator</code>.</li> <li>Database Migrations \u2014 prerequisite for default SQL storage.</li> </ul>"},{"location":"security/authorization/","title":"Authorization","text":"<p>The <code>Authorization</code> middleware enforces route-level access control. It reads security metadata from the matched <code>Route</code> and redirects or blocks requests that don't meet the requirements.</p>"},{"location":"security/authorization/#enable","title":"Enable","text":"<pre><code>$app-&gt;useAuthentication(); // must run before Authorization\n$app-&gt;useAuthorization();\n</code></pre> <p>Both must be enabled together \u2014 <code>Authorization</code> reads the identity stored by <code>Authentication</code>.</p>"},{"location":"security/authorization/#route-security-metadata","title":"Route security metadata","text":"<p>Routes declare their access requirements at definition time:</p> <pre><code>Route::create(\n    RouteMethod::Get,\n    Path::create('/dashboard'),\n    DashboardController::class,\n    'index',\n    authRequired: true,        // requires a logged-in user\n    roles: ['user', 'admin'],  // user must have at least one of these roles\n);\n\nRoute::create(\n    RouteMethod::Delete,\n    Path::create('/admin/users/{int:id}'),\n    AdminController::class,\n    'destroy',\n    authRequired: true,\n    roles: ['admin'],          // admin only\n);\n\nRoute::create(\n    RouteMethod::Get,\n    Path::create('/'),\n    HomeController::class,\n    'index',\n    // authRequired defaults to false \u2014 public route\n);\n</code></pre>"},{"location":"security/authorization/#authorization-rules","title":"Authorization rules","text":"Condition Result <code>authRequired: false</code> Route is public; middleware passes through. <code>authRequired: true</code>, no identity in context Redirect to <code>AuthSettings::signInPath</code> + clear auth cookie. <code>authRequired: true</code>, identity present, <code>roles: []</code> Any authenticated user is allowed. <code>authRequired: true</code>, identity present, role match Allowed. <code>authRequired: true</code>, identity present, no role match <code>403 Forbidden</code>."},{"location":"security/authorization/#configuring-the-sign-in-redirect","title":"Configuring the sign-in redirect","text":"<p>Register <code>AuthSettings</code> in your composition root:</p> <pre><code>use PhpMvc\\AuthSettings;\n\n$container-&gt;set(AuthSettings::class, new AuthSettings(\n    signInPath: '/account/sign-in',\n    cookieName: 'auth', // default; override if needed\n));\n</code></pre> <p>When an unauthenticated request hits a protected route, the middleware redirects to <code>signInPath</code> and clears the auth cookie.</p>"},{"location":"security/authorization/#role-model","title":"Role model","text":"<p>An authenticated user is allowed if their <code>Identity::getRoles()</code> intersects the route's <code>roles</code> array. An empty <code>roles</code> array means \"any authenticated user.\"</p>"},{"location":"security/csrf/","title":"CSRF Protection","text":"<p>The framework provides built-in Cross-Site Request Forgery protection via the <code>CsrfProtection</code> middleware.</p>"},{"location":"security/csrf/#enable","title":"Enable","text":"<pre><code>$app-&gt;useCsrfProtection();\n</code></pre> <p>Call this before <code>run()</code>.</p>"},{"location":"security/csrf/#how-it-works","title":"How it works","text":""},{"location":"security/csrf/#safe-methods-get-head-options","title":"Safe methods (GET, HEAD, OPTIONS)","text":"<p>A CSRF token is generated and made available for rendering in views.</p>"},{"location":"security/csrf/#unsafe-methods-post-put-patch-delete","title":"Unsafe methods (POST, PUT, PATCH, DELETE)","text":"<p>The middleware validates the CSRF token from one of two sources:</p> <ol> <li>Form body field \u2014 <code>_csrf</code></li> <li>HTTP header \u2014 <code>X-CSRF-Token</code></li> </ol> <p>If neither is present or the value doesn't match the session token, the middleware returns <code>403 Forbidden</code> with a short error message. The inner chain (controllers) is never reached.</p>"},{"location":"security/csrf/#reading-the-token-in-controllers","title":"Reading the token in controllers","text":"<pre><code>use PhpMvc\\Middlewares\\CsrfProtection;\nuse PhpMvc\\Requests\\RequestContext;\n\nfinal class FormController extends Controller\n{\n    public function __construct(\n        private readonly RequestContext $context,\n    ) {}\n\n    public function create(): ActionResponse\n    {\n        $token = CsrfProtection::getTokenFromContext($this-&gt;context);\n        return $this-&gt;view(model: ['csrfToken' =&gt; $token]);\n    }\n}\n</code></pre>"},{"location":"security/csrf/#adding-the-token-to-html-forms","title":"Adding the token to HTML forms","text":"<pre><code>&lt;form method=\"post\" action=\"/articles\"&gt;\n    &lt;input type=\"hidden\" name=\"_csrf\" value=\"{{model-&gt;csrfToken}}\"&gt;\n\n    &lt;label&gt;Title&lt;/label&gt;\n    &lt;input type=\"text\" name=\"title\"&gt;\n\n    &lt;button type=\"submit\"&gt;Create&lt;/button&gt;\n&lt;/form&gt;\n</code></pre>"},{"location":"security/csrf/#ajax-requests","title":"AJAX requests","text":"<p>For JavaScript-driven requests, read the token from a <code>&lt;meta&gt;</code> tag and send it as a header:</p> <pre><code>&lt;meta name=\"csrf-token\" content=\"{{csrfToken}}\"&gt;\n</code></pre> <pre><code>const token = document.querySelector('meta[name=\"csrf-token\"]').content;\n\nfetch('/api/articles', {\n    method: 'POST',\n    headers: {\n        'Content-Type': 'application/json',\n        'X-CSRF-Token': token,\n    },\n    body: JSON.stringify({ title: 'New article' }),\n});\n</code></pre>"},{"location":"security/csrf/#security-notes","title":"Security notes","text":"<ul> <li>The token is bound to the user session. Rotating the session (e.g. on sign-in) invalidates the old token.</li> <li>The <code>_csrf</code> body field takes precedence over the <code>X-CSRF-Token</code> header when both are present.</li> <li>Safe methods do not consume the token; the same token is valid across multiple GET requests within a session.</li> </ul>"},{"location":"security/identity-manager/","title":"Identity Manager","text":"<p><code>IdentityManager</code> is the contract between the authentication middleware and your application's user store. By default the framework uses a SQL-backed implementation, but you can replace it with your own.</p>"},{"location":"security/identity-manager/#default-sql-implementation","title":"Default SQL implementation","text":"<p>The package ships with <code>DefaultIdentityManager</code>, which uses PDO-backed repositories generated by <code>mvc auth:enable</code>. Register it with:</p> <pre><code>use PhpMvc\\Security\\Dependencies;\n\nDependencies::configure($container);\n</code></pre> <p>This binds:</p> <ul> <li><code>IdentityManager</code> \u2192 <code>DefaultIdentityManager</code></li> <li><code>UserIdentityRepository</code> \u2192 <code>SqlUserIdentityRepository</code></li> <li><code>SignInSessionRepository</code> \u2192 <code>SqlSignInSessionRepository</code></li> <li>Challenge repositories for sign-up and password reset</li> </ul> <p>The database schema is created by the migration generated via <code>mvc auth:enable</code>.</p>"},{"location":"security/identity-manager/#custom-identitymanager","title":"Custom <code>IdentityManager</code>","text":"<p>To use an external user store (LDAP, OAuth, a different database schema):</p> <ol> <li> <p>Enable authentication without migrations:</p> <pre><code>vendor/bin/mvc auth:enable --skip-migrations\n</code></pre> </li> <li> <p>Implement <code>IdentityManager</code>:</p> <pre><code>use PhpMvc\\Security\\IdentityManager;\nuse PhpMvc\\Security\\Identity;\n\nfinal class LdapIdentityManager implements IdentityManager\n{\n    public function __construct(\n        private readonly LdapClient $ldap,\n    ) {}\n\n    public function getIdentity(?string $token): Identity\n    {\n        $user = $token !== null ? $this-&gt;ldap-&gt;findByToken($token) : null;\n        return new MyIdentity($user);\n    }\n}\n</code></pre> </li> <li> <p>Register your implementation in the container (instead of calling <code>Dependencies::configure()</code>):</p> <pre><code>$container-&gt;set(IdentityManager::class, LdapIdentityManager::class);\n</code></pre> </li> </ol>"},{"location":"security/identity-manager/#challengenotificator","title":"<code>ChallengeNotificator</code>","text":"<p><code>Dependencies::configure()</code> does not register <code>ChallengeNotificator</code>. The sign-up and password-reset use cases depend on this interface to deliver one-time tokens to the user (by email, SMS, a queue, etc.). You must register a concrete implementation in your composition root:</p> <pre><code>$container-&gt;set(ChallengeNotificator::class, EmailChallengeNotificator::class);\n</code></pre>"},{"location":"security/identity-manager/#example-background-task-notificator","title":"Example: background-task notificator","text":"<p>A common pattern is to enqueue a background task instead of sending email synchronously:</p> <pre><code>use PhpMvc\\Security\\Domain\\Services\\ChallengeNotificator;\nuse PhpMvc\\Security\\Domain\\Entities\\SignUpChallenge;\nuse PhpMvc\\Security\\Domain\\Entities\\ResetPasswordChallenge;\nuse PhpMvc\\BackgroundTasks\\Application\\RegisterTask\\RegisterTask;\n\nfinal class BackgroundTaskChallengeNotificator implements ChallengeNotificator\n{\n    public function __construct(\n        private readonly RegisterTask $registerTask,\n    ) {}\n\n    public function sendSignUpChallenge(string $email, SignUpChallenge $challenge): void\n    {\n        $this-&gt;registerTask-&gt;execute(\n            type: 'send_sign_up_challenge_email',\n            payload: ['email' =&gt; $email, 'token' =&gt; $challenge-&gt;token],\n        );\n    }\n\n    public function sendResetPasswordChallenge(string $email, ResetPasswordChallenge $challenge): void\n    {\n        $this-&gt;registerTask-&gt;execute(\n            type: 'send_reset_password_challenge_email',\n            payload: ['email' =&gt; $email, 'token' =&gt; $challenge-&gt;token],\n        );\n    }\n}\n</code></pre>"},{"location":"security/identity-manager/#identity-interface","title":"<code>Identity</code> interface","text":"<p>Your identity object must implement the <code>Identity</code> contract:</p> <pre><code>interface Identity\n{\n    public function isAuthenticated(): bool;\n    public function username(): string;\n    public function getRoles(): array;\n    public function hasRole(string $role): bool;\n}\n</code></pre> <p><code>getRoles()</code> is used by the <code>Authorization</code> middleware to enforce route-level role restrictions.</p>"},{"location":"web/controllers/","title":"Controllers","text":"<p>Controllers are regular PHP classes that handle an HTTP action and return an <code>ActionResponse</code>. They are resolved from the PSR-11 container, so constructor injection works out of the box.</p>"},{"location":"web/controllers/#defining-a-controller","title":"Defining a controller","text":"<pre><code>&lt;?php\n\ndeclare(strict_types=1);\n\nnamespace App\\MyApp\\Controllers;\n\nuse PhpMvc\\Controllers\\Controller;\nuse PhpMvc\\Actions\\Responses\\ActionResponse;\nuse PhpMvc\\Actions\\Responses\\View;\nuse PhpMvc\\Actions\\Responses\\RedirectTo;\nuse PhpMvc\\Actions\\Responses\\LocalRedirectTo;\n\nfinal class ArticleController extends Controller\n{\n    public function __construct(\n        private readonly ArticleRepository $articles,\n    ) {}\n\n    public function index(): ActionResponse\n    {\n        $articles = $this-&gt;articles-&gt;findAll();\n        return $this-&gt;view(model: (object) ['articles' =&gt; $articles]);\n    }\n\n    public function show(int $id): ActionResponse\n    {\n        $article = $this-&gt;articles-&gt;findById($id);\n        return $this-&gt;view(model: $article);\n    }\n\n    public function store(ArticleRequest $request): ActionResponse\n    {\n        $this-&gt;articles-&gt;save($request);\n        return $this-&gt;redirectToAction('index');\n    }\n}\n</code></pre>"},{"location":"web/controllers/#actionresponse-subtypes","title":"<code>ActionResponse</code> subtypes","text":""},{"location":"web/controllers/#view","title":"<code>View</code>","text":"<p>Renders an HTML template. The view path is resolved relative to the configured views directory.</p> <pre><code>// Renders Views/Article/index.html (convention: ControllerName/action)\nreturn $this-&gt;view();\n\n// Explicit path (no .html extension)\nreturn $this-&gt;view(name: 'Article/list');\n\n// Pass a model\nreturn $this-&gt;view(model: $article);\n</code></pre> <p>The <code>Controller::view()</code> helper infers the view path from the calling class and method name when not given explicitly.</p>"},{"location":"web/controllers/#redirectto","title":"<code>RedirectTo</code>","text":"<p>Redirects to an absolute URL (http/https only):</p> <pre><code>return RedirectTo::create(url: 'https://example.com/external');\nreturn RedirectTo::create(url: 'https://example.com/new-path', args: ['ref' =&gt; 'old']);\n</code></pre> <p>The status code is always <code>302 Found</code>.</p>"},{"location":"web/controllers/#localredirectto","title":"<code>LocalRedirectTo</code>","text":"<p>Redirects to another controller/action within the same app. The URL is resolved automatically \u2014 you never hardcode paths.</p> <p>Use the <code>redirectToAction()</code> helper from <code>Controller</code>:</p> <pre><code>// Same controller, no args\nreturn $this-&gt;redirectToAction('index');\n\n// Different controller, with args\nreturn $this-&gt;redirectToAction(\n    action: 'show',\n    controller: ArticleController::class,\n    args: (object) ['id' =&gt; $newId],\n);\n</code></pre> <p>Or call <code>LocalRedirectTo::create()</code> directly when outside a controller:</p> <pre><code>return LocalRedirectTo::create(\n    action: 'show',\n    controller: ArticleController::class,\n    args: (object) ['id' =&gt; $newId],\n);\n</code></pre>"},{"location":"web/controllers/#wiring-to-routes","title":"Wiring to routes","text":"<p>Controllers are referenced by their fully-qualified class name in <code>Route::create()</code>. There is no annotation magic \u2014 the link between a route and a controller is explicit:</p> <pre><code>Route::create(\n    RouteMethod::Get,\n    Path::create('/articles/{int:id}'),\n    ArticleController::class, // FQCN\n    'show',                   // method name as string\n);\n</code></pre>"},{"location":"web/controllers/#action-parameter-resolution","title":"Action parameter resolution","text":"<p>Action arguments are resolved automatically from the request:</p> <ol> <li>Route path parameters \u2014 type-coerced from the URL.</li> <li>Query string \u2014 scalar keys mapped by parameter name.</li> <li>Parsed body \u2014 for <code>POST</code>/<code>PUT</code>/<code>PATCH</code>/<code>DELETE</code> requests.</li> <li><code>ServerRequestInterface</code> \u2014 injected automatically if the action declares it.</li> <li>DTO objects \u2014 non-scalar parameters are constructed from request input.</li> </ol> <p>See Request Binding for the full binding rules.</p>"},{"location":"web/middleware/","title":"Middleware","text":"<p>The framework uses a PSR-15 middleware pipeline. Middlewares run in a fixed outer order; custom middlewares are inserted inside that order, just before <code>RequestHandler</code>.</p>"},{"location":"web/middleware/#pipeline-order","title":"Pipeline order","text":"<pre><code>flowchart TD\n    EH[\"ErrorHandling\\n(always outermost)\"]\n    Loc[\"Localization\\n(always second)\"]\n    Custom[\"Custom middlewares\\n(your order via addMiddleware)\"]\n    CSRF[\"CsrfProtection\\n(optional)\"]\n    Auth[\"Authentication\\n(optional)\"]\n    Authz[\"Authorization\\n(optional)\"]\n    RH[\"RequestHandler\\n(always innermost)\"]\n\n    EH --&gt; Loc --&gt; Custom --&gt; CSRF --&gt; Auth --&gt; Authz --&gt; RH</code></pre> <p>Request flows top-to-bottom; response propagates back up.</p>"},{"location":"web/middleware/#built-in-middlewares","title":"Built-in middlewares","text":""},{"location":"web/middleware/#errorhandling","title":"<code>ErrorHandling</code>","text":"<p>Always the outermost middleware. Catches any uncaught <code>Throwable</code> from the inner chain and converts it to an HTTP error response using the mapping registered in <code>ErrorSettings</code>. Ensures the browser always receives a well-formed HTTP response.</p>"},{"location":"web/middleware/#localization","title":"<code>Localization</code>","text":"<p>Detects the request locale (from <code>Accept-Language</code> header or a locale cookie) and stores it in <code>RequestContext</code>. The view engine reads this to resolve i18n keys.</p>"},{"location":"web/middleware/#csrfprotection-opt-in","title":"<code>CsrfProtection</code> (opt-in)","text":"<p>Enable with:</p> <pre><code>$app-&gt;useCsrfProtection();\n</code></pre> <ul> <li>Safe methods (<code>GET</code>, <code>HEAD</code>, <code>OPTIONS</code>): generates a CSRF token and stores it in <code>RequestContext</code>. Does not block.</li> <li>Unsafe methods (<code>POST</code>, <code>PUT</code>, <code>PATCH</code>, <code>DELETE</code>): validates the token from body field <code>_csrf</code> or header <code>X-CSRF-Token</code>. Returns <code>403</code> on failure.</li> </ul>"},{"location":"web/middleware/#authentication-opt-in","title":"<code>Authentication</code> (opt-in)","text":"<p>Enable with:</p> <pre><code>$app-&gt;useAuthentication();\n</code></pre> <p>Reads the auth cookie, calls <code>IdentityManager::getIdentity()</code>, and stores the result in <code>RequestContext</code>. Does not block unauthenticated requests by itself.</p>"},{"location":"web/middleware/#authorization-opt-in","title":"<code>Authorization</code> (opt-in)","text":"<p>Enable with:</p> <pre><code>$app-&gt;useAuthorization();\n</code></pre> <p>Reads the current route's <code>authRequired</code> and <code>roles</code> from the matched <code>Route</code>. Redirects unauthenticated users to <code>AuthSettings::signInPath</code> (clearing the auth cookie). Returns <code>403</code> for authenticated users who lack the required role.</p>"},{"location":"web/middleware/#adding-custom-middlewares","title":"Adding custom middlewares","text":"<p>Implement PSR-15 <code>MiddlewareInterface</code>:</p> <pre><code>use Psr\\Http\\Message\\ResponseInterface;\nuse Psr\\Http\\Message\\ServerRequestInterface;\nuse Psr\\Http\\Server\\MiddlewareInterface;\nuse Psr\\Http\\Server\\RequestHandlerInterface;\n\nfinal class RateLimitMiddleware implements MiddlewareInterface\n{\n    public function process(\n        ServerRequestInterface $request,\n        RequestHandlerInterface $handler,\n    ): ResponseInterface {\n        // inspect request...\n\n        return $handler-&gt;handle($request); // pass through\n    }\n}\n</code></pre> <p>Register in your app entrypoint before calling <code>run()</code>:</p> <pre><code>$app-&gt;addMiddleware(RateLimitMiddleware::class);\n</code></pre> <p>The middleware is resolved from the container, so constructor injection works. Multiple <code>addMiddleware()</code> calls run in registration order.</p>"},{"location":"web/request-binding/","title":"Request Binding","text":"<p>Action method parameters are resolved and type-coerced automatically from the incoming request. You rarely need to touch the raw request object.</p>"},{"location":"web/request-binding/#resolution-order","title":"Resolution order","text":"<p>For each parameter of a controller action, the framework checks sources in this order:</p> <ol> <li>Route path parameters \u2014 extracted and typed from the URL (e.g. <code>{int:id}</code> \u2192 <code>int $id</code>).</li> <li>Query string \u2014 scalar parameters matched by name from <code>?key=value</code>.</li> <li>Parsed request body \u2014 for <code>POST</code>, <code>PUT</code>, <code>PATCH</code>, <code>DELETE</code> requests.</li> <li>PSR-7 request \u2014 if the parameter type is <code>ServerRequestInterface</code>, the request object is injected directly.</li> <li>DTO / object \u2014 any non-scalar class is treated as a request object and constructed from the flat input.</li> </ol>"},{"location":"web/request-binding/#scalar-normalization","title":"Scalar normalization","text":"<p>All scalar values are automatically normalized:</p> PHP type Behavior <code>string</code> Trimmed. <code>int</code> Digits-only strings cast to <code>int</code>; non-numeric input \u2192 <code>null</code>. <code>float</code> Numeric strings cast to <code>float</code>; non-numeric \u2192 <code>null</code>. <code>bool</code> <code>\"1\"</code>, <code>\"true\"</code>, <code>\"on\"</code>, <code>\"yes\"</code> \u2192 <code>true</code>; <code>\"0\"</code>, <code>\"false\"</code>, <code>\"off\"</code>, <code>\"no\"</code> \u2192 <code>false</code>. <p>Returning <code>null</code> for invalid numeric input lets your action or DTO detect missing/malformed data explicitly:</p> <pre><code>public function show(?int $id): ActionResponse\n{\n    if ($id === null) {\n        return $this-&gt;view(name: 'errors/not-found', statusCode: StatusCode::NotFound);\n    }\n    // ...\n}\n</code></pre>"},{"location":"web/request-binding/#dto-binding","title":"DTO binding","text":"<p>Non-scalar parameters are constructed using named constructor arguments:</p> <pre><code>final class CreateArticleRequest\n{\n    public function __construct(\n        public readonly string $title,\n        public readonly string $body,\n        public readonly int $categoryId,\n    ) {}\n}\n\n// Controller action:\npublic function store(CreateArticleRequest $request): ActionResponse { ... }\n</code></pre> <p>The input keys <code>title</code>, <code>body</code>, and <code>categoryId</code> (from the request body) are mapped to constructor parameters by name.</p>"},{"location":"web/request-binding/#dotted-keys-for-nested-objects","title":"Dotted keys for nested objects","text":"<pre><code>final class AddressRequest\n{\n    public function __construct(\n        public readonly string $street,\n        public readonly string $city,\n    ) {}\n}\n\nfinal class ShippingRequest\n{\n    public function __construct(\n        public readonly AddressRequest $address,\n    ) {}\n}\n</code></pre> <p>HTML form fields:</p> <pre><code>&lt;input name=\"address.street\" value=\"123 Main St\"&gt;\n&lt;input name=\"address.city\" value=\"Springfield\"&gt;\n</code></pre>"},{"location":"web/request-binding/#array-binding","title":"Array binding","text":"<pre><code>final class BatchRequest\n{\n    /**\n     * @param array&lt;TagRequest&gt; $tags\n     */\n    public function __construct(\n        public readonly array $tags,\n    ) {}\n}\n</code></pre> <p>HTML:</p> <pre><code>&lt;input name=\"tags[0][name]\" value=\"php\"&gt;\n&lt;input name=\"tags[1][name]\" value=\"mvc\"&gt;\n</code></pre> <p>The <code>@param array&lt;Type&gt;</code> docblock is required to infer the array element type.</p>"},{"location":"web/request-binding/#injecting-the-psr-7-request","title":"Injecting the PSR-7 request","text":"<p>For cases where you need the raw request (e.g. reading headers):</p> <pre><code>use Psr\\Http\\Message\\ServerRequestInterface;\n\npublic function upload(ServerRequestInterface $request): ActionResponse\n{\n    $contentType = $request-&gt;getHeaderLine('Content-Type');\n    // ...\n}\n</code></pre> <p>The framework detects the <code>ServerRequestInterface</code> type hint and injects the current request object directly.</p>"},{"location":"web/routing/","title":"Routing","text":"<p>The routing API maps URL patterns and HTTP methods to controller actions. Routes are registered in the <code>router()</code> method of your <code>MvcWebApp</code> subclass.</p>"},{"location":"web/routing/#core-classes","title":"Core classes","text":"Class Role <code>PhpMvc\\Routes\\Router</code> Collection of routes; matches an incoming request to a <code>Route</code>. <code>PhpMvc\\Routes\\Route</code> A single route: method + path + controller + action + security metadata. <code>PhpMvc\\Routes\\RouteMethod</code> Enum of HTTP methods: <code>Get</code>, <code>Post</code>, <code>Put</code>, <code>Patch</code>, <code>Delete</code>, <code>Head</code>, <code>Options</code>. <code>PhpMvc\\Routes\\Path</code> Compiled path pattern with typed parameter extraction."},{"location":"web/routing/#registering-routes","title":"Registering routes","text":"<pre><code>use PhpMvc\\Routes\\Router;\nuse PhpMvc\\Routes\\Route;\nuse PhpMvc\\Routes\\RouteMethod;\nuse PhpMvc\\Routes\\Path;\n\n$router = new Router();\n\n$router-&gt;register(Route::create(\n    RouteMethod::Get,\n    Path::create('/'),\n    HomeController::class,\n    'index',\n));\n\n$router-&gt;register(Route::create(\n    RouteMethod::Get,\n    Path::create('/articles/{int:id}'),\n    ArticleController::class,\n    'show',\n));\n\n$router-&gt;register(Route::create(\n    RouteMethod::Post,\n    Path::create('/articles/{int:id}/comments'),\n    CommentController::class,\n    'store',\n    authRequired: true,\n    roles: ['user', 'admin'],\n));\n</code></pre>"},{"location":"web/routing/#typed-path-parameters","title":"Typed path parameters","text":"<p><code>Path</code> supports four typed segment placeholders. The type is validated at match time, so an invalid value (e.g. a string where <code>{int:id}</code> is expected) results in a 404.</p> Syntax PHP type Validation <code>{name}</code> <code>string</code> Any non-empty segment <code>{int:name}</code> <code>int</code> Digits only (casted to <code>int</code>) <code>{float:name}</code> <code>float</code> Numeric (casted to <code>float</code>) <code>{uuid:name}</code> <code>string</code> UUID v4 format <p>Example paths:</p> <pre><code>/users/{int:id}          \u2192 int $id\n/files/{uuid:token}      \u2192 string $token (validated UUID)\n/products/{slug}         \u2192 string $slug\n/prices/{float:amount}   \u2192 float $amount\n</code></pre>"},{"location":"web/routing/#security-metadata","title":"Security metadata","text":"<pre><code>Route::create(\n    RouteMethod::Post,\n    Path::create('/admin/users/{int:id}/delete'),\n    AdminController::class,\n    'delete',\n    authRequired: true,\n    roles: ['admin'],\n);\n</code></pre> <ul> <li><code>authRequired: true</code> \u2014 the <code>Authorization</code> middleware redirects unauthenticated requests to <code>AuthSettings::signInPath</code>.</li> <li><code>roles</code> \u2014 an array of allowed role names. An authenticated user must have at least one matching role from <code>Identity::getRoles()</code>. An empty <code>roles</code> array allows any authenticated user.</li> </ul>"},{"location":"web/routing/#route-matching-flow","title":"Route matching flow","text":"<pre><code>flowchart LR\n    Req[\"Incoming request\\nmethod + path\"]\n    Method[\"Filter by\\nHTTP method\"]\n    Pattern[\"Match path\\npattern\"]\n    Types[\"Validate\\nparameter types\"]\n    Found[\"Route matched\\n\u2192 invoke action\"]\n    NotFound[\"404\"]\n\n    Req --&gt; Method\n    Method --&gt;|subset of routes| Pattern\n    Pattern --&gt;|pattern matches| Types\n    Types --&gt;|types valid| Found\n    Types --&gt;|type mismatch| NotFound\n    Pattern --&gt;|no match| NotFound</code></pre>"},{"location":"web/views/","title":"Views &amp; Templates","text":"<p>The framework uses a lightweight template engine. Templates are plain <code>.html</code> files \u2014 no PHP execution inside templates.</p>"},{"location":"web/views/#template-language-reference","title":"Template language reference","text":"Syntax Description <code>{{var}}</code> HTML-escaped variable output. <code>{{obj-&gt;prop}}</code> Access object property (HTML-escaped). <code>{{arr.key}}</code> Access array key (HTML-escaped). <code>{{{var}}}</code> Raw (unescaped) HTML \u2014 use only for already-sanitized content. <code>{{#if cond:}} ... {{#endif cond:}}</code> Conditional block. <code>cond</code> is a model key. <code>{{#for item in items:}} ... {{#endfor items:}}</code> Loop over an array. <code>{{#layout name:}}</code> Wrap this template in a layout file. <code>{{content}}</code> Inside a layout: marks where the child template is injected. <code>{{i18nKey}}</code> Resolved from <code>{locale}.json</code> in <code>i18nBasePath</code>."},{"location":"web/views/#variable-output","title":"Variable output","text":"<pre><code>&lt;!-- HTML-escaped (safe for all user-supplied values) --&gt;\n&lt;p&gt;Hello, {{user-&gt;name}}!&lt;/p&gt;\n\n&lt;!-- Raw HTML (only for sanitized/trusted content) --&gt;\n&lt;div&gt;{{{article-&gt;htmlBody}}}&lt;/div&gt;\n</code></pre>"},{"location":"web/views/#conditionals","title":"Conditionals","text":"<pre><code>{{#if isAdmin:}}\n  &lt;a href=\"/admin\"&gt;Admin panel&lt;/a&gt;\n{{#endif isAdmin:}}\n</code></pre> <p>The condition key is resolved from the model/data passed to the view. A truthy value (non-empty string, non-zero number, <code>true</code>) shows the block.</p>"},{"location":"web/views/#loops","title":"Loops","text":"<pre><code>&lt;ul&gt;\n{{#for article in articles:}}\n  &lt;li&gt;\n    &lt;a href=\"/articles/{{article-&gt;id}}\"&gt;{{article-&gt;title}}&lt;/a&gt;\n  &lt;/li&gt;\n{{#endfor articles:}}\n&lt;/ul&gt;\n</code></pre>"},{"location":"web/views/#layouts","title":"Layouts","text":"<p><code>layout.html</code>:</p> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n  &lt;title&gt;{{pageTitle}}&lt;/title&gt;\n  &lt;link rel=\"stylesheet\" href=\"/{{mainCssBundler}}\"&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;nav&gt;&lt;!-- shared nav --&gt;&lt;/nav&gt;\n  &lt;main&gt;\n    {{content}}\n  &lt;/main&gt;\n  &lt;script src=\"/{{mainJsBundler}}\"&gt;&lt;/script&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p>Child template <code>Views/Article/show.html</code>:</p> <pre><code>{{#layout layout:}}\n\n&lt;article&gt;\n  &lt;h1&gt;{{article-&gt;title}}&lt;/h1&gt;\n  &lt;p&gt;{{article-&gt;body}}&lt;/p&gt;\n&lt;/article&gt;\n</code></pre>"},{"location":"web/views/#internationalization","title":"Internationalization","text":"<pre><code>&lt;button&gt;{{save_button_label}}&lt;/button&gt;\n</code></pre> <p><code>assets/i18n/en.json</code>:</p> <pre><code>{\n  \"save_button_label\": \"Save changes\"\n}\n</code></pre> <p><code>assets/i18n/es.json</code>:</p> <pre><code>{\n  \"save_button_label\": \"Guardar cambios\"\n}\n</code></pre> <p>The locale is detected automatically from the request and the correct translation file is loaded.</p>"},{"location":"web/views/#passing-data-to-views","title":"Passing data to views","text":"<p>From a controller action:</p> <pre><code>return $this-&gt;view(model: $article, data: [\n    'pageTitle' =&gt; $article-&gt;title,\n    'breadcrumbs' =&gt; [['Home', '/'], ['Articles', '/articles']],\n]);\n</code></pre> <ul> <li><code>model</code> \u2014 available as <code>{{model-&gt;...}}</code> in the template.</li> <li><code>data</code> \u2014 merged into the top-level template context; keys are available directly as <code>{{pageTitle}}</code>.</li> </ul>"},{"location":"web/views/#security","title":"Security","text":"<p>All <code>{{var}}</code> output is HTML-escaped via <code>htmlspecialchars()</code>. Only <code>{{{var}}}</code> skips escaping. Treat <code>{{{...}}}</code> as you would <code>echo $htmlString</code> \u2014 only use it for content you control or have already sanitized.</p>"}]}